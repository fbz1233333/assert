#pragma once

#include "fxcc/graph/pch.h"

namespace fxcc
{
	namespace graph
	{
		template <typename T>
		struct Mesh;

		template <typename T>
		struct Texture2D;

		template <typename T>
		struct TextureCube;

		template <typename T>
		struct Texture2DArray;

		template <typename T>
		struct Shader;

		template <typename T>
		struct RenderTarget;

		template <typename T>
		struct Effect2d;

		template <typename T>
		struct Effect3d;

		template <typename T>
		struct RenderStates;

		template <typename T>
		struct PipelineState;

		template <typename T>
		struct Factory;

		template <typename T>
		struct ImEditor;

		template <typename T>
		struct TypeDesc;
	}
}